extends ./layout.jade
block contenido
	div(class="center-it")
		div(class="center-it")
			form(action="/users",method="POST")
				div(class="form-group")
					label(for="email") Email
					input(type="text", name="email", placeholder="Email",id="email", class="form-control")
				div
					label(for="password") Password
					input(type="password", name="password", placeholder="Password",id="password", class="form-control")
				div
					label(for="password") Confirma tu password
					input(type="password", name="password_confirmation", placeholder="Confirma Password",id="password_confirmation", class="form-control")
				div
					label(for="username") Confirma tu password
					input(type="text", name="username", placeholder="Username",id="username", class="form-control")
				div(class="text-right")
					a(href="/login") Login
				div
					input(type="submit" value="Iniciar sesion" class="btn btn-info")